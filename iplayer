#!/usr/bin/env bash
set -e

LOCAL="$(cd "$(dirname "$0")"; pwd)"

echo ================================================================================
echo "$1"
echo ================================================================================

"$LOCAL"/iplayer_radio "$1"
"$LOCAL"/iplayer_tv "$1"
